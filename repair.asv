count=8;
for i=0:108
    fl=('E:\Document\MATLAB\'+string(count)+'/EyeTracking/'+string(i)+'.txt');
    opts=detectImportOptions(fl); % サンプルデータのデータ形式判定
    EyeSpot=readmatrix(fl, opts);% サンプルデータを表として読み込む
    while 1
        con=con+1;
        EyeSpot(con,1)=1;
    if EyeSpot(con)<EyeSpot(con,5)
        EyeSpot(con,1)=3;
        con=0;
        break;
    end
    end
    eyedata=[eyedata;EyeSpot];
    disp(i);   
end